# 企业搜索结果治理

`POST /api/enterprise/searchai/result-governance` 对混合检索后的候选结果执行租户隔离、文档 ACL、数据分类、受限访问审批、个人信息脱敏和相关度门禁。

通过安全过滤的结果会按相关度排序并受最大返回数量限制；系统同时计算引用覆盖率和数据源多样性，返回 `RELEASE`、`REVIEW` 或 `BLOCKED`。被拒结果只保留文档编号与原因，不会把正文送入生成模型。

该功能适用于企业站内搜索和 RAG 前置检索层，可与现有索引发布、搜索质量评估组合成“索引—召回—授权—引用”闭环。
